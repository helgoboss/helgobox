import {Mapping} from "../generated/realearn"

const mapping: Mapping = {
    source: {
        type: "MidiDisplay",
        spec: {
            type: "MackieLcd",
            channel: 5,
            line: 2
        }
    }
}